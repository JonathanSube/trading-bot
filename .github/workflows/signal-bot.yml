name: Signal Bot

on:
  schedule:
    # Fallback-Takt wie beim ORB-Bot (.github/workflows/trading-bot.yml) -
    # native GitHub-Schedules laufen unzuverlaessig nach (siehe
    # trading-bot-spec.md, Betriebsvorfaelle), Haupt-Auslöser ist der
    # externe cron-job.org-Trigger auf workflow_dispatch, idealerweise
    # dichter getaktet fuer zeitnahe Signalreaktion. Minuetlich, 06:55-21:00
    # UTC deckt beide Sessions inkl. 5-Minuten-Vorlauf ab (Nutzerwunsch
    # 27.08.2026): EU-Vorlauf ab 06:55 UTC (5 Min. vor 09:00 Europe/Berlin im
    # Sommer, im Winter entsprechend frueher lokal aber innerhalb dieses
    # UTC-Fensters), US-Vorlauf ab 13:25 UTC (5 Min. vor 09:30 America/
    # New_York im Sommer) bis zur bisherigen grosszuegigen Pauschale um
    # 21:00 UTC - siehe EU_SESSION_START_LOCAL/US_SESSION_START_LOCAL/
    # PRE_SESSION_LEAD_MINUTES in scripts/run_signal_bot.py, die das
    # tatsaechliche Ein-/Ausschalten pro Lauf DST-sicher entscheiden.
    - cron: "* 6-21 * * 1-5"
  workflow_dispatch: {}

concurrency:
  group: signal-bot
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - run: pip install -r requirements.txt

      - name: Signal-Bot-Durchlauf
        env:
          ALPACA_API_KEY: ${{ secrets.ALPACA_API_KEY }}
          ALPACA_SECRET_KEY: ${{ secrets.ALPACA_SECRET_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
          TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
          TELEGRAM_USER_SESSION: ${{ secrets.TELEGRAM_USER_SESSION }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          SIGNAL_CHANNEL: ${{ secrets.SIGNAL_CHANNEL }}
        run: python scripts/run_signal_bot.py

      - name: Zustand und Protokoll zurückschreiben
        run: |
          git config user.name "signal-bot"
          git config user.email "actions@users.noreply.github.com"
          for f in signal_state.json signal_trades.csv; do
            [ -f "$f" ] && git add "$f"
          done
          git diff --staged --quiet || git commit -m "Signal-Bot-Lauf $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          git push
